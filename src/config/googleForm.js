/**
 * Jal-Seva Google Form – embedded directly on the registration page.
 * Form: https://forms.gle/PRBsXMhrmS7pREtEA
 *
 * For best embedding, set GOOGLE_FORM_EMBED_URL to the long viewform URL + ?embedded=true
 * (Get it: open the form → Send → <> Embed → copy the iframe src).
 * If left empty, the short link is used (may redirect inside the iframe).
 */

export const GOOGLE_FORM_URL = 'https://forms.gle/PRBsXMhrmS7pREtEA'

/**
 * Embed URL for the iframe. Should look like:
 * https://docs.google.com/forms/d/e/1FAIpQLS.../viewform?embedded=true
 * Leave empty to use the short link (GOOGLE_FORM_URL) in the iframe.
 */
export const GOOGLE_FORM_EMBED_URL = ''
